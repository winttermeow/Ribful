>
> # ${\color{red}Список\space изменений}$
>


## `          V 0.6a          ` $${\color{aqua} 2026-01-25}$$

`+` **Массово** улучшены текстуры и UI игры;

`+!` 8й уровень;

`+!` Добавлена система привязки клавиш;

`+` Добавлена система "Статистики", которая учитывает почти всё, что вы делали в игре (Доступно в параметрах);

`+!` Игра теперь доступна на двух языках (ru & en), осуществлен полный перевод;

`+` Добавлена система всплывающих подсказок при наведении курсором;

`+` Полностью переделано меню кастомизации, теперь в зеркале можно зайти в специальное меню;

`+!!!` **НОВЫЙ РЕЖИМ: "РЕЗНЯ"** - Убивай волны монстров и прокачивайся с каждым уровнем с помощью специальных карт!;

`+` Бесконечное множество минорных и средних исправлений/улучшений.





## `          V 0.5.05          ` $${\color{aqua} 2025-10-01}$$

`+` Исправление багов;

`+` Новые анимации у скелета.

## `          V 0.5.04          ` $${\color{aqua} 2025-10-01}$$


`+!` В главном меню добавлено зеркало, позволяющее кастомизировать персонажа! На данный момент доступно:
> 17 Шапок
>
> 5 Ботинок

`+` Увеличен и слегка изменен интерфейс в режиме "Костолом"

## `          V 0.5.03          ` $${\color{aqua} 2025-09-27}$$

`+!` В игре теперь __рабочая__ пауза;

`+` Обновлен интерфейс в некоторых местах;

`+!` Спрайт больших и маленьких шипов увеличен с 9x9 до 11x11, а так же шипы теперь выровнены по их хитбоксу (почти), тем самым они стали меньше визуально, но сам хитбокс никак не изменился;

`+` Улучшены параметры:

> 1) Удобный интерфейс;
>
> 2) Настройка времени перехода между сценами;
>
> 3) Функция начала игры только при нажатии (Очень полезно для спидраннеров);
>
> 4) Сброс всех настроек в игре.

`+` Прицел у снайперской винтовки теперь стал плавнее на 60+ герц мониторах. Теперь по мышам стрелять еще приятнее :ribLike: ;

`+!` Все возможные настройки в игре сохраняются, даже если в нее перезайти;

`+!` Переработка некоторых формул в костоломе:

> 1) Чем выше сложность, тем ЕЩЕ БОЛЬШЕ вы получаете очков, увеличение по очкам стало намного выше, то есть - теперь есть смысл играть на очень высокой сложности, ведь дают намного больше очков, чем раньше! (Особенно за новый этаж);
>
> 2) Формула счета за кости переделана, теперь дают много только на высокой сложности;
>
> 3) Формула ежесекундного счета тоже переделана, теперь в ней важны убийства мышей и собранные кости.

`+` 8 уровень слегка изменен. (Все еще в разработке);

`+` Добавлен новый моб/ловушка - "Одуванчик" (Пока-что есть только в уровне 8, можете посмотреть).

## `          V 0.5.02          ` $${\color{aqua} 2025-09-23}$$

`+` Баг с исчезающим курсором после прохождения уровня должен быть пофикшен.

`+` Улучшена вкладка параметры (Возможность изменить размер окна игры и сделать ее в окне, а также настройка времени рестарта [R] )

`+` Добавлены всплывающие надписи в Костоломе, показывающие счёт, который игрок получает.

`+` В Костоломе спрайты лазера и мыши были изменены.

`+` Размеры мышей в Костоломе рандомизированы.

`+` Изменен некоторый текст в интерфейсе Костолома.

`+` При прохождении уровня появляется случайная маленькая надпись в конце.

`+` Минимальная сложность в Костоломе для занесения в таблицу лидеров изменена с 250 до 320!

## `          V 0.5.01          ` $${\color{aqua}  2025-09-13}$$

`+` Таймер по запросам продлен на еще 1 цифру после запятой, новый формат: --:--,---;

`+` У меню уровней новый формат;

`+` Лазер в Костоломе замедлен в 4 раза;

`+` Каждую секунду И за убийство летучих мышей дают очки;

`+` Таймер в Хрустяшках теперь показывает нормальное время, а не ускоренное в 2 раза;

`+` Виды кнопок и текста изменены повсюду (Надеюсь);

`+!!!` Добавлены таблицы лидеров внутри игры! Они разделены на:

> Уровни (Без смертей, со смертями)
>
> Костолом
>
> Хрустяшки (Там же, где и уровни)
>
> Общий счет среди всех игроков (Таблица высчитывает количество ваших мест со всех возможных таблиц и присваивает общий счет)

## `          V 0.5          ` $${\color{aqua}  2025-08-30}$$

`+` 1 новый уровень.

`+` Теперь абсолютно в каждом из уровней есть секретка;

`+` В режиме "Хрустяшки" теперь кости появляются разного размера;

`+` Был произведен респрайт текстур Сюрикена и Лезвия;

`+` Добавлены два новых тайла: "Затемнение" и "Мох";

`+` Добавлены звуки на кнопки в главном меню;

`+` Добавлена реклама телеграм-канала в гланом меню :ribLike: ;

`+` Переделана и улучшена пост-обработка;

`+` Пофикшено огромное кол-во багов и глитчей;


`!!!!!+` **НОВЫЙ РЕЖИМ - "КОСТОЛОМ"** - Бесконечная башня, со случайной генерацией этажей, поднимающейся снизу лавой и гибкой настройкой этого всего, а именно:

> - Процент заполнения обычными блоками;
> - Процент заполнения шипами;
> - Кол-во появляющихся ловушек;
> - Настройки скорости и размеров ловушек;
> - Полная настройка летающих мышей (Самое страшное что вы можете встретить);
> - Настройка параметров лавы;
> - Возможность дать игроку снайперскую винтовку (В моде на v0.4 она уже была, а тут я ее все-таки официально добавил в игру);

`!` Настройки генерации распространяются на параметры одного этажа;

`!` Все ваши настройки влияют на параметр "Сложность", чем сложнее вы делаете генерацию, тем выше этот параметр. "Сложность" влияет на "Счёт", который вы получаете по мере прохождения башни;

`!` За каждый новый этаж (50 метров) вы получаете щит на 3 секунды, который делает вас полностью неуязвимым;

`!` __Все__ ловушки убивают мышь в этом режиме, но можно сделать ее невосприимчивой к ним (То есть, можно будет убить только снайперской винтовкой).

## `          V 0.4          ` $${\color{aqua} 2023-02-25}$$

`+` Добавлен койотджамп на прыжки от стен (После разворота во время скольжения по стене у вас будет несколько кадров чтобы совершить прыжок, раньше моментально делался даблджамп, сильно раздражало людей)

`+` Добавлена функция рестарта для спидраннеров (Зажать "R" на 0.5 сек).
`+` Новый вид таймера. (Миллисекунды)

`+` Изменено несколько спрайтов.

`+` На тестовой карте теперь в теории можно сделать ран без смертей.

`+` Добавлен Джампбафферинг на прыжок/прыжок от стены
(Если вы ещё не коснулись пола/стены, но нажали прыжок за 0,2с./0,1с. до столкновения с полом/стеной, то прыжок всё равно произойдет)
P.S. это всех бесило, по этому теперь вы не будете ругаться на меня что прыжок не зарегало.

`+` Сделан новый режим, для кого-то он покажется скучным, но он просто для расслабона

`+` Улучшил экран завершения уровня. Теперь вместо ""Level 1" пройден!" пишется "Уровень 1 пройден!", аналогично к остальным.

`+` Хитбокс шипов теперь в виде такой вот трапеции, раньше был вытянутый шестиугольник.

`+` Убрал x32 версию, а то зачем она вообще нужна?...

`+` Теперь маловероятно, что вы с гумбой убьёте друг-друга. Скорее он тебя. Так же исправлена регулировка высоты при отскоке от гумбы, из-за нее раньше казалось что вы слабо от него отпрыгиваете или же просто не отпрыгнули (Но на деле вы просто отжали пробел сразу же после отскока от гумбы, тем самым "регулируя" высоту прыжка). Теперь высота прыжка от головы гумбы можно сказать фиксирована.

`!!!+` Теперь в игре 6 уровней. 2 и 3 сдвинуты на 4 и 5, соответственно первые 3 теперь дейтвительно не сложные, а вот 4, 5 и 6 сложные (6 вообще нереально сложный)

## `          V 0.3          ` $${\color{aqua} 2023-02-18}$$

`+` Некоторые места на тестовой карте теперь легче;

`+` Изменен хитбокс убивающего блока, теперь он на пиксель меньше со всех сторон.

`+` Добавлен таймер;

`+` Изменён дизайн меню;

`+` Отображение локации (Уровня) в Дискорде;

`+` Изменена иконка игры;

`+` Тряска экрана при смерти игрока;

`+` Фикс стрельбы пушек;
(Теперь они стреляют в своем направлении, а не только вправо)

`+` Загрузочный экран;

`+` Возможность выходить в меню с помощью клавиши Escape;
(Подобие паузы)

`+` Улучшен спрайт кнопок;

`+` Игра теперь для 32- и 64-разрядных систем.


`!` В игре теперь есть звуки и музыка, громкость которых можно настроить в меню и паузе;

`!` Я создал 3 уровня для прохождения;

`!` Чтобы пройти уровень нужно заполучить кубок, находящийся на последнем этапе уровня;

`!` Вывод статистики и финального счёта за прохождение уровня;
Формула счёта:
> Кость: +55
> 
> Смерть: -10
> 
> Секунда времени: -1

`!` Создана Гугл Таблица, где будут списки лидеров, учитывая прохождение со смертями и без смертей (На скорость/счёт). #╔🕑о-спидранах 

`!` Настройки сохраняются при переходе по уровням, а также их изменение теперь видно прямо в меню;

`!` Тестовую карту теперь можно пройти;

`!` Изменен хитбокс игрока;
(Теперь у него менее закругленные края, что позволит меньше соскальзывать с краёв блоков)

`!` Фиксированная максимальная скорость падения;
(Больше не разгоняется до скорости света при падении)

`!` Изменение движения камеры, а также её оптимизация.
(Более быстрое слежение за игроком, отключение фокусировки в сторону куда смотрит игрок. Так же присутствует эффект её тряски)
## `          V 0.2          ` $${\color{aqua}2023-02-17}$$

`+` Discord Rich Presence;

`+` Больше контента на карте;

`+` Пара пасхалок;

`+` У чекпоинтов прикольная анимация.

## `          V 0.1          ` $${\color{aqua}2023-02-17}$$

`+` Возможность выключить фон и пост-обработку (Небольшая оптимизация);

`+` Койот джампинг (У вас будет несколько кадров для того чтобы совершить прыжок, если вы сошли с блока, например при разбеге перед пропастью. Раньше если хоть немного сходишь с блока делался даблджамп, что очень бесило людей);

`+` Совсем чуть-чуть нового контента на карте.
